# Workign directory for PDL course

This uses a conda environment called pdl
So far installed is:

```
conda install jupyter
conda install -c fastchan fastai
```

This is CPU only afaik, which suits me just fine.


